[Unit]
Description=Generate unique SSH host keys for this device
Before=ssh.service

[Service]
Type=oneshot
UMask=0077
ExecStart=/usr/bin/ssh-keygen -A
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
